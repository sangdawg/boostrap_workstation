#### How to use this on a new machine

Install Rclone: ```sudo dnf install rclone```

Authenticate with the rclone remote (eg OneDrive or Google Drive): Run ```rclone config``` to create your remote (ensure the name matches the script).

Run the script: ```./ootb_rclones_config.sh```
